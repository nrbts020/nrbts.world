---
// Sticky magazine chrome — logo mark (clipped-corner square, per the
// design system) + sparse top-right links. This is an editorial site
// first, so the nav is just the two things that actually exist: Articles
// (the /serial index) and Shop (the concept merch page). Contact was
// dropped — a "#" link to nowhere doesn't belong on a site whose whole
// pitch is being honest about what's real. The mobile burger is a
// static glyph, not wired to a menu — this is a portfolio piece, not a
// live product, and the nav links collapse away under 820px per the
// design system's own rule.
---

<header class="nav">
	<a class="logo" href="/serial">
		<span class="mk">S</span>
		<span class="nm">serial</span>
	</a>
	<nav class="links">
		<a href="/serial">Articles</a>
		<a href="/serial/shop">Shop</a>
	</nav>
	<button class="burger" aria-label="menu">&#9776;</button>
</header>
